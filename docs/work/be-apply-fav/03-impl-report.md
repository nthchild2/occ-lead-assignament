# Implementation Report · be-apply-fav

- **Feature id:** `be-apply-fav`
- **Inputs read:** `00-spec.md`, `02-plan.md`
- **Implementer:** implementer subagent (full-auto)
- **Date:** 2026-07-01

## Changes made

| File (`path`)                                                                  | What changed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | R-ids                   | Plan step |
| ------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- | --------- |
| `backend/src/domains/applications/applications.schema.ts` (new)                | Re-exports `Application`/`ApplicationListResponse` from `@occ/shared`; adds backend-only `JobIdParamsSchema = z.object({ id: z.string() })` for the `:id` path param                                                                                                                                                                                                                                                                                                                                     | R1, R3, R4, R9          | 1         |
| `backend/src/domains/favorites/favorites.schema.ts` (new)                      | Re-exports `FavoriteListResponse`/`Job` from `@occ/shared`; adds backend-only `JobIdParamsSchema = z.object({ id: z.string() })`                                                                                                                                                                                                                                                                                                                                                                         | R5, R6, R7, R9          | 2         |
| `backend/src/domains/applications/applications.service.ts` (new)               | Express-free `createApplicationsService({ getJob })` factory; module-level `Map<userId, Map<jobId, Application>>`; `apply` (calls `getJob` for 404, throws `ALREADY_APPLIED` on dupe, else stores `{ jobId, appliedAt, job }`), `cancel` (throws `NOT_FOUND` if absent, else deletes), `list` (returns `{ items }`)                                                                                                                                                                                      | R1, R2, R3, R4, R9      | 3         |
| `backend/src/domains/favorites/favorites.service.ts` (new)                     | Express-free `createFavoritesService({ getJob })` factory; module-level `Map<userId, Set<jobId>>` (ids only); `favorite` (calls `getJob` for 404, throws `ALREADY_FAVORITED` on dupe, else adds id), `unfavorite` (throws `NOT_FOUND` if absent, else removes), `list` (maps ids through `getJob` into `{ items: Job[] }`)                                                                                                                                                                               | R5, R6, R7, R9          | 4         |
| `backend/src/domains/applications/applications.router.ts` (new)                | Express-only file; `createApplicationsActionsRouter({ getJob })` (`POST`/`DELETE /:id/apply`) and `createApplicationsListRouter({ getJob })` (`GET /`); both apply `authMiddleware` + `validate({ params: JobIdParamsSchema })` where relevant + the `if (!req.user) throw AppError('AUTH_REQUIRED', …)` guard mirrored from `auth.router.ts`; delegate to a `createApplicationsService({ getJob })` instance; respond via `success(res, …)`                                                             | R1, R2, R3, R4, R8, R9  | 5         |
| `backend/src/domains/favorites/favorites.router.ts` (new)                      | Mirror of the applications router: `createFavoritesActionsRouter({ getJob })` (`POST`/`DELETE /:id/favorite`) and `createFavoritesListRouter({ getJob })` (`GET /`), same auth-guard + validate + success pattern                                                                                                                                                                                                                                                                                        | R5, R6, R7, R8, R9      | 6         |
| `backend/src/app.ts` (edit)                                                    | Imported `jobsService` and the four new router factories; added `const getJob = jobsService.getById`; mounted `createApplicationsActionsRouter({ getJob })` and `createFavoritesActionsRouter({ getJob })` at `/jobs` (after the existing public `jobsRouter`), and `createApplicationsListRouter({ getJob })` / `createFavoritesListRouter({ getJob })` at `/applications` / `/favorites`, all above `app.use(errorMiddleware)`; removed the two commented placeholder lines                            | R9, R10                 | 7         |
| `backend/src/domains/applications/__tests__/applications.router.test.ts` (new) | Supertest integration tests against the mounted `app`; `loginAndGetToken()` copied from `auth.router.test.ts`; pins `jobs[0].id` (`job-01`) and bogus id `'does-not-exist'`. Covers apply → duplicate (409 `ALREADY_APPLIED`) → cancel (200) → re-cancel (404 `NOT_FOUND`); apply to unknown job (404); unauthenticated apply/cancel (401 `AUTH_REQUIRED`); unauthenticated `GET /applications` (401); `GET /applications` body parses via `ApplicationListResponseSchema` with the embedded job present | R1, R2, R3, R4, R8, R11 | 8         |
| `backend/src/domains/favorites/__tests__/favorites.router.test.ts` (new)       | Supertest integration tests, same structure, pins `jobs[1].id` (`job-02`). Covers favorite → duplicate (409 `ALREADY_FAVORITED`) → remove (200) → re-remove (404 `NOT_FOUND`); favorite on unknown job (404); unauthenticated favorite/unfavorite (401); unauthenticated `GET /favorites` (401); `GET /favorites` body parses via `FavoriteListResponseSchema` (full `Job` items)                                                                                                                        | R5, R6, R7, R8, R11     | 9         |

## Traceability

| R-id | Satisfied by (file)                                                                                                     | Notes                                                                |
| ---- | ----------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| R1   | `applications.schema.ts`, `applications.service.ts`, `applications.router.ts`, `applications.router.test.ts`            | `POST /jobs/:id/apply`; 404 propagates from injected `getJob`        |
| R2   | `applications.service.ts`, `applications.router.test.ts`                                                                | `ALREADY_APPLIED` (409) on duplicate apply                           |
| R3   | `applications.schema.ts`, `applications.service.ts`, `applications.router.ts`, `applications.router.test.ts`            | `DELETE /jobs/:id/apply`; `NOT_FOUND` (404) if no application        |
| R4   | `applications.schema.ts`, `applications.service.ts`, `applications.router.ts`, `applications.router.test.ts`            | `GET /applications` → `{ data: { items } }`, each item embeds `job`  |
| R5   | `favorites.schema.ts`, `favorites.service.ts`, `favorites.router.ts`, `favorites.router.test.ts`                        | `POST /jobs/:id/favorite`; 404 / `ALREADY_FAVORITED` (409)           |
| R6   | `favorites.schema.ts`, `favorites.service.ts`, `favorites.router.ts`, `favorites.router.test.ts`                        | `DELETE /jobs/:id/favorite`; `NOT_FOUND` (404) if not favorited      |
| R7   | `favorites.schema.ts`, `favorites.service.ts`, `favorites.router.ts`, `favorites.router.test.ts`                        | `GET /favorites` → `{ data: { items } }`, full `Job` items           |
| R8   | `applications.router.ts`, `favorites.router.ts` (`authMiddleware` + `if (!req.user)` guard on all 6 routes)             | Verified 401 `AUTH_REQUIRED` in both test files                      |
| R9   | `applications.service.ts`, `favorites.service.ts` (`getJob` injected, no `domains/jobs` import), `app.ts` (sole wiring) | Manual grep confirms no `domains/jobs` import outside `app.ts`/tests |
| R10  | `backend/src/app.ts`                                                                                                    | Four routers mounted above `errorMiddleware`, which stays last       |
| R11  | `applications.router.test.ts`, `favorites.router.test.ts`                                                               | 62/62 backend tests pass, including both new suites                  |

## Deviations from the plan

**None.** All 9 "Handoff to IMPLEMENT" steps were executed as specified. Router factory names (`createApplicationsActionsRouter` / `createApplicationsListRouter` / `createFavoritesActionsRouter` / `createFavoritesListRouter`) follow `02-plan.md` changes #5–#6 verbatim (the invocation prompt's shorthand `createApplicationsRouter({ getJob })` was a summary; the plan is the authoritative artifact and specifies two factories per domain — actions + list — which is what was built).

## Anything left for a follow-up

- App-side stores/screens (`activity-stores`, `activities-screen`) consuming these endpoints — explicitly out of scope per `00-spec.md` ("Explicitly out of scope").
- In-memory state does not persist across restarts or multiple instances — sanctioned tech debt (A1 Decision 5), not addressed here.

## Handoff to VERIFY

- Workspaces touched (for `tsc`): `backend` (also re-verify `packages/shared` unaffected — no edits made there).
- New/changed tests to run: `backend/src/domains/applications/__tests__/applications.router.test.ts`, `backend/src/domains/favorites/__tests__/favorites.router.test.ts` (and the full backend suite for regressions).
- Anything the verifier should pay special attention to:
  - Confirm no `domains/jobs` import exists in `applications.service.ts`/`favorites.service.ts`/`applications.router.ts`/`favorites.router.ts` (eslint's `import/no-restricted-paths` zones do NOT cover `applications|favorites → jobs`, only `→ auth`, so this boundary is enforced by code review, not lint — confirmed clean by `grep -rn "domains/jobs" backend/src --include='*.ts'` excluding `app.ts` and `__tests__`).
  - `pnpm --filter '@occ/backend' run typecheck` — 0 errors.
  - `pnpm lint` — 0 errors (12 pre-existing `security/detect-object-injection` warnings, none in new files).
  - `env -u JWT_SECRET pnpm --filter '@occ/backend' run test` — 9 suites / 62 tests passed, including the 2 new suites.
